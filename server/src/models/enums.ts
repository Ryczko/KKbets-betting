enum BetTypes {
    WINNER = 'winner'
}

enum UserTypes {
    HOME = 'home',
    DRAW = 'draw',
    AWAY = 'away'
}

enum EventsStates {
    PENDING = 'pending',
    LOST = 'lost',
    WINNING = 'winning'
}

export { BetTypes, UserTypes, EventsStates };
