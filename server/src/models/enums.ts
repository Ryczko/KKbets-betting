enum BetTypes {
    MAIN_RESULT = 'main result'
}

enum UserTypes {
    HOME = 'home',
    DRAW = 'draw',
    AWAY = 'away'
}

enum EventsStates {
    PENDING = 'pending',
    LOST = 'lost',
    WINNING = 'winning'
}

export { BetTypes, UserTypes, EventsStates };
