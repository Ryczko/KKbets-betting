import styled from 'styled-components';

export const StyledBanner = styled.div`
    max-width: 100%;
    position: relative;

    overflow: hidden;

    img {
        width: 100%;
        display: block;
        height: 300px;
        border-radius: 10px;
        object-fit: cover;
    }
`;
