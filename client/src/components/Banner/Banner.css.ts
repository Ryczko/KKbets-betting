import styled from 'styled-components';

export const StyledBanner = styled.div`
    img {
        width: 100%;
        display: block;
        max-height: 390px;
        border-radius: 4px;
        object-fit: cover;
    }
`;
